# =============================================================================
# SensorShield Backend Dependencies
# Minimum version bounds — compatible with Python 3.12+, 3.13+, 3.14+
# Install with: pip install -r requirements.txt --prefer-binary
# =============================================================================

# --- Web Framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --- Data Validation ---
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Database ---
sqlalchemy>=2.0.30
alembic>=1.13.0
asyncpg>=0.29.0          # async PostgreSQL driver (FastAPI routes)
psycopg2-binary>=2.9.0   # sync PostgreSQL driver (Alembic migrations)
python-dotenv>=1.0.0

# --- ML Core ---
numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.5.0

# --- Deep Learning (install separately — large package) ---
# torch>=2.3.0           # run: pip install torch --index-url https://download.pytorch.org/whl/cpu

# --- Gradient Boosting (downstream predictor baseline) ---
xgboost>=2.0.0

# --- Explainability (install after scikit-learn is confirmed working) ---
# shap>=0.45.0

# --- Experiment Tracking ---
# mlflow>=2.13.0

# --- IoT / Messaging ---
paho-mqtt>=2.0.0

# --- HTTP Client ---
httpx>=0.27.0

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
aiosqlite>=0.20.0        # SQLite async driver for test DB

# --- Utilities ---
python-dateutil>=2.9.0
tqdm>=4.66.0
